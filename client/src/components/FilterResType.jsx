//This function should display
//the resource type filter bar
//And should also filter the resource
//based on the type the user selected
export default function FilterResType(){
    return(<div>Remove here</div>)
}